name: echemdb
channels:
  - conda-forge
  - defaults
dependencies:
  - mkdocs
  - pip
  - pandas
  - matplotlib
  - plotly
  - pip:
    - mkdocs-material
    - markdown-include
    - mdutils
    - mkdocs-section-index
    - mkdocs-macros-plugin
    - python-frontmatter
    - tabulate
    - datapackage
    - frontmatter

